# Task 19

import numpy as np

# Example usage:

x = np.zeros((300, 20))  # Create a matrix of shape (300, 20)
print(f"Original shape: {x.shape}")

# Transpose the matrix

x = np.transpose(x)
print(f"Transposed shape: {x.shape}")
